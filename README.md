## Features
- generate object reports including full object list, coin object list, coin count per level
- sorted objects ordered by type and importance
- move to the next object with the arrow keys
- better object names

### Smaller things
- cleaner names and order in level select
- orbit camera by default
- increased zoom speed from mouse wheel